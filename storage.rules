rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Encrypted assets use signed URLs or server-side Admin SDK access.
    match /{path=**} {
      allow read, write: if false;
    }
  }
}
