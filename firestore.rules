rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Private Pro browser code uses authenticated server procedures. The Admin SDK
    // bypasses these rules for account bootstrap, encrypted vault data, and assets.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
